function table9to9() {
    for (let i = 1; i <= 9; i++) {
        for (let k = 1; k <= 9; k++) {
            // let count = i * k;
            // console.log(i + `*` + k + `=` + `${count}`)
            console.log(i + `*` + k + `=` + i * k)
        }
    }
}
table9to9();